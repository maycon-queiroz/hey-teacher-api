#!/usr/bin/env sh

# rodar os testes
 php artisan test --parallel;
 if [ $? -ne 0 ]; then
     echo "Opa! Deu ruim aqui com algum teste. Arrume antes de continuar... 😉";
     exit 1;
 fi;

exit 0;
